{
	"name": "consent-management-caliper-benchmark",
	"version": "2.0.0",
	"description": "Hyperledger Caliper benchmark suite for the ConsentMD consent management system",
	"scripts": {
		"provision-identities": "node setup/provision-identities.js",
		"benchmark": "./run-benchmarks.sh",
		"benchmark:suite": "npx caliper launch manager --caliper-workspace ./ --caliper-networkconfig networks/fabric/bench-network.yaml --caliper-benchconfig benchmarks/consent-management-suite.yaml --caliper-flow-only-test",
		"benchmark:smoke": "./run-single-benchmark.sh minimal-consent-granting",
		"aggregate": "node src/aggregate-results.js",
		"test": "node test/workload.test.js"
	},
	"keywords": [
		"hyperledger",
		"caliper",
		"fabric",
		"blockchain",
		"benchmark",
		"consent-management",
		"healthcare"
	],
	"author": "ConsentMD Team",
	"license": "Apache-2.0",
	"dependencies": {
		"@grpc/grpc-js": "^1.10.3",
		"@hyperledger/caliper-cli": "^0.6.0",
		"@hyperledger/caliper-core": "^0.6.0",
		"@hyperledger/caliper-fabric": "^0.6.0",
		"@hyperledger/fabric-gateway": "^1.5.0",
		"fabric-ca-client": "^2.2.20",
		"fabric-common": "^2.2.20"
	},
	"engines": {
		"node": ">=18.0.0",
		"npm": ">=8.0.0"
	}
}
